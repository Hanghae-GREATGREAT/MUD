interface InputForm {
    input: number;
}

interface dungeonInfoForm {
    dungeonNo: number;
    name: string;
    recommendLevel: string;
    script: string;
}
[];

export { InputForm, dungeonInfoForm };
