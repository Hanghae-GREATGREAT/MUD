import { UserSession } from "../interfaces/user"


export function questHandler(CMD: string | undefined, user: UserSession) {

    const script = ''
    const field = ''
    return { script, user, field };
}